# ===================================
# FRONTEND ENVIRONMENT VARIABLES
# ===================================
# React app frontend configuration
# For production, set this in Vercel dashboard or use .env.production

# Backend API URL (use local for development, production URL for deployed frontend)
REACT_APP_API_URL=http://localhost:3001

# ===================================
# BACKEND ENVIRONMENT VARIABLES
# ===================================
# Backend reads: .env, .env.local (here), and ../.env.local (project root)

# Get your Gemini API key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your gemini api key here

# Deepgram API key for audio transcription (free: 25 hrs/month)
# Sign up at https://console.deepgram.com and get your API key
DEEPGRAM_API_KEY=your deepgram api key here

# Backend server port
PORT=3001

# Optional: request timeout in ms (default 120000 = 2 min)
REQUEST_TIMEOUT_MS=120000

# Optional: yt-dlp YouTube authentication cookies (recommended for production).
# Provide one of the options below when YouTube returns "Sign in to confirm you're not a bot".
# 1) Path to mounted cookies file in Netscape format
# YTDLP_COOKIES_FILE=/etc/secrets/youtube_cookies.txt
#
# 2) Base64-encoded Netscape cookies file content
# YTDLP_COOKIES_BASE64=
#
# 3) Raw Netscape cookies file content (\n escapes are supported for single-line env values)
# YTDLP_COOKIES=
